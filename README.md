# img_size_formatter
Takes a hierarchical folder path as input. Recursively finds all images inside all sub-folders. Converts every image to a fixed size. Preserves the exact same folder hierarchy in the output. Creates a ZIP file containing all resized images.
